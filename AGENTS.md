# design-system

**In one line:** Multi-app component bank that harvests UI from SISO and third-party apps, then promotes pieces to primitives, composites and systems with shared adapters. District: `Great_Library_of_SISO` (`~/SISO_Workspace/Great_Library_of_SISO/knowledge/design-system`).

The rest is in `README.md`. Find anything else on this machine with `estate where <words>`.

{
  "name": "siso-design-system",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build:manifest": "node scripts/build-manifest.mjs",
    "prebuild": "node scripts/build-manifest.mjs",
    "install": "cd viewer && npm install --legacy-peer-deps",
    "build": "node scripts/build-manifest.mjs && cd viewer && npm run pages:build"
  }
}
